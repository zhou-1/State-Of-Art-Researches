All commands about Git.    


At the end:       
For the change to be shareable, you need to commit it:   
``` 
git commit -m "comments add here"
```
Now you can push it to your remote git repository:   
```
git push
```
